# dizzie

dizzie is a Terminal User Interface for [dizz](https://github.com/TheShiveshNetwork/dizz).

```bash
dizz ui           # launch from dizz (auto-installs if missing)
dizzie            # or run directly
```
